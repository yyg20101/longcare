import org.gradle.api.Plugin
import org.gradle.api.Project

class KotlinCommonConventionPlugin : Plugin<Project> {
    override fun apply(target: Project) {
        // Intentionally minimal for phased migration.
    }
}
